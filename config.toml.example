# Конфигурация Terminal Emulator
# Файл должен быть расположен по пути: ~/.config/terminal-emulator/config.toml

[window]
# Ширина окна в пикселях (по умолчанию: 800)
width = 900

# Высота окна в пикселях (по умолчанию: 600)
height = 650

# Прозрачность окна от 0.0 (полностью прозрачное) до 1.0 (непрозрачное)
# По умолчанию: 1.0
opacity = 0.92

# Стиль заголовка окна (по умолчанию: standard)
# Доступные варианты:
#   "standard" - стандартный системный заголовок
#   "integrated" - монолитный заголовок того же цвета что и терминал, без контура
headerbar_style = "integrated"

# Надпись
title = ""

# Толщина обводки окна в пикселях (по умолчанию: 0 - без обводки)
border_width = 5

# Цвет обводки окна в формате #RRGGBB (по умолчанию: #ffffff)
# Применяется только если border_width > 0
# Примеры:
#   #ffffff - белый
#   #00ff00 - зелёный
#   #ff0000 - красный
#   #00d9ff - голубой
# Градиент (формат: "linear_gradient to [направление] #color1 #color2 ..."):
#   "linear_gradient to right #ff0000 #00ff00 #0000ff" - горизонтальный градиент
#   "linear_gradient to bottom #ff00ff #00ffff" - вертикальный градиент
#   "linear_gradient to right bottom #ff0000 #00ff00" - диагональный градиент
# Градиенты теперь полностью совместимы с border_radius!
border_color = "linear_gradient to right bottom #3b0745 #240745 #260b90 #2709a1"

# Скругление углов окна в пикселях (по умолчанию: 0 - без скругления)
# Примеры:
#   0 - прямые углы
#   10 - слегка скруглённые углы
#   20 - заметно скруглённые углы
border_radius = 15

[terminal]
# Цвет фона терминала в формате #RRGGBB
# По умолчанию: #1e1e1e (тёмно-серый)
# Примеры:
#   #000000 - чёрный
#   #1e1e1e - тёмно-серый
#   #282c34 - One Dark
#   #002b36 - Solarized Dark
background_color = "#1b1e1f"
